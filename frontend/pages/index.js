import Link from 'next/link';
import Items from '../components/Items';
import styled from 'styled-components';

const Home = (props) => {
	return (
		<div>
			<Items />
		</div>
	);
};

export default Home;
